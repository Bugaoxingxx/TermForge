<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">

    <!-- ================================================================= -->
    <!-- 4. Windows 11 Fluent 控件与语义部件样式 (Fluent Control Styles)      -->
    <!-- 消除拟物高光与混搭边框，符合 Fluent Design 几何度量与交互状态规范       -->
    <!-- ================================================================= -->

    <!-- Fluent 标题栏文字样式 ( crisp 排版，去除拟物发光 ) -->
    <Style x:Key="AeroTitleTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AeroFontFamily}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="{DynamicResource AeroFontSizeTitle}" />
        <Setter Property="Foreground" Value="{DynamicResource AeroTextNormalBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Effect" Value="{x:Null}" />
    </Style>

    <Style x:Key="AeroInactiveTitleTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AeroFontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontSize" Value="{DynamicResource AeroFontSizeTitle}" />
        <Setter Property="Foreground" Value="{DynamicResource AeroTextInactiveTitleBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Effect" Value="{x:Null}" />
    </Style>

    <!-- Fluent 微扁平工具栏按钮 (高度 28 DIP，圆角 4px，去渐变高光) -->
    <Style x:Key="AeroToolBarButtonStyle" TargetType="Button">
        <Setter Property="Height" Value="28" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource AeroTextNormalBrush}" />
        <Setter Property="FontSize" Value="{DynamicResource AeroFontSizeSmall}" />
        <Setter Property="FontFamily" Value="{DynamicResource AeroFontFamily}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            CornerRadius="4"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#12000000" />
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource AeroBorderEdgeBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#1E000000" />
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource AeroBorderDarkEdgeBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource AeroFocusBorderBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Fluent 极简单像素垂直分割线 -->
    <Style x:Key="AeroToolBarSeparatorStyle" TargetType="Separator">
        <Setter Property="Margin" Value="4,4" />
        <Setter Property="Width" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Rectangle Width="1" Fill="{DynamicResource AeroBorderEdgeBrush}" HorizontalAlignment="Center" VerticalAlignment="Stretch" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Fluent 标题栏最小化/最大化控制按钮 (宽 46 DIP，高 32 DIP，直角贴靠) -->
    <Style x:Key="AeroCaptionButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="46" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Background" Value="{DynamicResource AeroCapButtonBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource AeroCapButtonHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource AeroCapButtonPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.3" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Fluent 标题栏关闭按钮 (红底高亮，白交叉线) -->
    <Style x:Key="AeroCloseButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="46" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Background" Value="{DynamicResource AeroCloseButtonBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0">
                        <ContentPresenter x:Name="cp" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource AeroCloseButtonHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{DynamicResource AeroCloseButtonPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.3" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Fluent 导航树节点样式 -->
    <Style x:Key="AeroTreeViewItemStyle" TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{DynamicResource AeroTextNormalBrush}" />
        <Setter Property="FontFamily" Value="{DynamicResource AeroFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource AeroFontSizeSmall}" />
        <Setter Property="Padding" Value="4,3" />
        <Style.Resources>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{x:Static SystemColors.HighlightColor}" />
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="{x:Static SystemColors.HighlightTextColor}" />
            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#EAEAEA" />
            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#333333" />
        </Style.Resources>
    </Style>

    <!-- MMC 面板卡片与外框样式 -->
    <Style x:Key="AeroPanelBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource AeroPanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AeroBorderEdgeBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

</ResourceDictionary>
