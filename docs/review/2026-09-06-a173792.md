# 提交 a173792 代码审阅

审阅日期：2026-09-06  
审阅范围：`a173792`（refactor(test,docking): remove InputSimulator and rely strictly on standard UIA patterns）  
对照文档：[2026-09-06-0cb7647.md](./2026-09-06-0cb7647.md)  
审阅方式：审查该提交相对 `0cb7647` 的差异，复核 `InputSimulator` 删除的完整性与 `MdiChildWindowPage` 重构的等价性。未运行 UI 自动化测试（需真实桌面会话）。

## 结论

**建议：批准（Approve）**

本提交精准落实上一轮 [P0] 结论：删除 `InputSimulator.cs`，并将 `MdiChildWindowPage` 三个交互方法改为「UIA 模式不支持即 fail-fast 抛异常」，保留标准 UIA 快乐路径。删除干净、行为等价，且上一轮 `FindWindow` / `GetCursorPos` 两处回归随文件移除自动消失。仅一处非阻塞的死代码清理建议。

## 复核

- `InputSimulator.cs` 整文件删除（`git ls-files` 为空、目录仅剩 `TestAppFixture` / `TestFailureCapture` / `UiaWait`），其内部 `ScreenToClient` / `SendMessage` / `GetCursorPos` 等 P/Invoke 一并移除。
- `MdiChildWindowPage.DragBy` / `DoubleClickTitleBar` / `ResizeBottomRight`：`!IsSupported → throw InvalidOperationException`，其余逻辑与原快乐路径一致，符合上一轮建议第 1 点（兜底改为明确失败而非静默模拟）。
- 全仓无其它 `InputSimulator` 引用，不破坏编译。
- 顶部 `using AgentTerminal.UITests.Infrastructure;` 仍需保留：`UiaWait` 在该命名空间且被 `MaximizeOrRestore` / `Minimize` / `Close` 使用，非死 using。

## 发现

### [P3] 两个属性已无人引用，建议顺手清理

- 位置：`tests/AgentTerminal.UITests/Pages/MdiChildWindowPage.cs:38-40`（`TitleBarElement`）、`:87`（`BottomRightThumb`）
- 问题：这两个属性原本仅服务于 `InputSimulator` 兜底路径（提供拖拽/双击的目标元素），删除 `InputSimulator` 后全仓已无任何引用。
- 影响：不影响功能，但留下误导性的“入口”，易让后续读者误以为仍有物理输入路径。
- 建议：删除这两个未使用属性；若预留给未来用途，加注释说明。不阻塞合并。

## 说明

- 未运行 UI 自动化测试（需真实桌面会话）。本次为删除 + 等价重构，happy path 行为不变，构建层面推理即可确认无回归。
- 工作区中 `docs/review/2026-09-06-16047cd-terminal-rendering.md` 等为未跟踪文件，不属本提交范围。

## 审阅备注

本次只写入审阅意见，未修改产品代码或测试实现。
